git init - will initalize an empty git repository in the specified folder with .git extention (it is a hidden path)

intermediate area - where the current edits of file(s) are not tracked by .git

staging area - where your current edits will be tracked
git add {filename}
